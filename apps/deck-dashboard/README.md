# deck-dashboard — system monitor TUI

Real-time system dashboard for the cyberdeck using Textual.

Shows: CPU temp, memory/swap, disk usage, uptime, load average,
network status, and top processes. Refreshes every 2 seconds.

```
./run.sh
```

Keybindings: `R` refresh, `T` dark mode, `Ctrl+Q` quit.
